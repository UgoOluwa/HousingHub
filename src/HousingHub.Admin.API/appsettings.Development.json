{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//Secrets": "Never commit real values. Use user-secrets locally: dotnet user-secrets set \"AdminJwt:Secret\" \"<32+ random chars>\" --project src/HousingHub.Admin.API",
  "//Cors": "Localhost origins live here so they never reach the production allow-list.",
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://localhost:3000",
      "http://localhost:3001"
    ]
  },
  "Dynamo": {
    "//AutoCreateTables": "Local development: create any missing tables at startup so a fresh DynamoDB Local works out of the box.",
    "AutoCreateTables": true,
    "//UsePublishedIndex": "Reads published listings from PublishedStatus-index instead of scanning Properties. MUST stay false until every published row has been re-saved so it appears in the sparse index - see docs/data-backfill-required.md. Switching this on early empties the homepage.",
    "UsePublishedIndex": false
  },
  "//Sentry": "Dsn empty = monitoring off, app unaffected. Set it per environment (AWS Lambda env var Sentry__Dsn). Use a DIFFERENT Sentry project for each API.",
  "Sentry": {
    "Dsn": "",
    "Environment": "development"
  }
}
